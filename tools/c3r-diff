#!/bin/sh
top=`git rev-parse --show-toplevel`
color=never
if [ -t 1 ]
then
    num_colors=`tput colors`
    if [ "$num_colors" ] && [ "$num_colors" -ge 8 ]
    then color=always
    fi
fi
git diff --color="$color" "$@" | python "$0.py" "$top/users" | less -R
